[user]
	name = Alejandro
	email = jbernal@unal.edu.co

[user]
	hide = update-index --assume-unchanged
    unhide = update-index --no-assume-unchanged
	hidden = ! git ls-files -v | grep '^h' | cut -c3-
