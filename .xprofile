#to import env variables to dbus
dbus-update-activation-environment --systemd DBUS_SESSION_BUS_ADDRESS DISPLAY XAUTHORITY
# transmission-daemon &
dunst &

fcitx & # for languages

# custom scripts
startupScripts

DISPLAY=":0" picom -b
# compton &
dwmstatus &
xrdb -merge ~/.Xresources
copyq &
# setup monitors
# cm
autorandr --change
wal -R &
sxhkd &

# lastScheme=$(ls -t "$HOME/.cache/wal/schemes" | sed 1q)
# wal -f "$HOME/.cache/wal/schemes/${lastScheme}"
