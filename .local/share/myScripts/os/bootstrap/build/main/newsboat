#!/bin/sh

if [ ! -e "$CARGO_HOME" ]; then
    $OS/build/main/rust
fi

cd "$PROGRAMS"
# declare -A req=(\
# ['eopkg']='stfl-devel curl-devel sqlite3-devel json-c-devel'\
# ['zypper']='libxml2-devel'\
# )
# install_ ${req[$PKG]}

# gem install asciidoctor
# sudo -u "$SUDO_USER" wget https://newsboat.org/releases/2.22.1/newsboat-2.22.1.tar.xz
# tar kvvxf "newsboat-2.22.1.tar.xz"
# rm "newsboat-2.22.1.tar.xz"
# cd "newsboat-2.22.1"
# make
# make install
