#!/bin/sh

cd "$PROGRAMS"
#Clone the repo
sudo -u "$SUDO_USER" git clone --depth=1 https://github.com/sdushantha/fontpreview

# Change your current directory to fontpreview
cd fontpreview

# Install it
make install
